{
	"build": { "dockerfile": "Dockerfile" },

	"customizations": {
		"vscode": {
			"extensions": ["dbaeumer.vscode-eslint", "esbenp.prettier-vscode", "arcanis.vscode-zipfs"]
		}
	},

	"onCreateCommand": "yarn install --immutable"
}
